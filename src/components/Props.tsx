/* eslint-disable */
// All possible values currently found in a React component's (props)
export interface Props {
  ballot?: any;
  voteTime?: string;
  hash?: string;
  sha?: string;
  classes?: any;
  match?: any;
  history?: any;
}
